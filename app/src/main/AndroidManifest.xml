<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ── -->

    <!-- Foreground service (all Android versions) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Foreground service type declaration (Android 14+ / API 34) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Detect current network type (WiFi/LTE/5G) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Restart collection service after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Show foreground service notification (Android 13+ / API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Per-app usage stats via NetworkStatsManager (requires Settings grant) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Signal strength reading on some Android versions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- ── Application ── -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataHarvester"
        tools:targetApi="36">

        <!-- Onboarding / Permission Gate (LAUNCHER) -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard (launched from Onboarding) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!-- Data Collection Foreground Service -->
        <service
            android:name=".DataCollectionService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Boot Receiver — restarts collection after reboot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>